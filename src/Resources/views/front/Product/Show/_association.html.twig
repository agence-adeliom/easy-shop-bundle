{% if product_association.associatedProducts|length %}
    <h4>{{ product_association.type.name }}</h4>
    <hr>
    <div id="sylius-product-association-{{ product_association.type.name }}" {{ sylius_test_html_attribute('product-association', product_association.type.name) }}>
        {% include '@EasyShop/front/Product/_horizontalList.html.twig' with {'products': product_association.associatedProducts} %}
    </div>
{% endif %}
