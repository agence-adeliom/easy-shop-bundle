{% if product.enabledVariants.empty() or product.simple and not sylius_inventory_is_available(product.enabledVariants.first) %}
    {% include '@EasyShop/front/Product/Show/_outOfStock.html.twig' %}
{% else %}
    {{ render(url('sylius_shop_partial_cart_add_item', {'template': '@EasyShop/front/Product/Show/_addToCart.html.twig', 'productId': product.id})) }}
{% endif %}
